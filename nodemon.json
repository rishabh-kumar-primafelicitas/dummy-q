{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "node --import tsx --no-warnings ./src/server.ts",
  "env": {
    "NODE_OPTIONS": "--no-warnings"
  }
}